# Deprecated components

> [!WARNING]
> **Not available in this beta version**: The components in this folder (Zigbee and OpenThread) do not work in this beta version of Minino. These components are only available in versions prior to 1.1.13.0.
>
> **To use Zigbee and OpenThread components**: If you need to use Zigbee or OpenThread functionality, you must checkout the version 1.1.13.0 using:
> ```bash
> git checkout 1.1.13.0
> ```

At the moment, the components you find here on this folder are not compatible with the esp-idf 5.5.1 version used in this beta, but they will be updated in the future.

**Included components:**
- Zigbee: Components related to Zigbee (esp-zigbee-console, zigbee_switch, etc.)
- OpenThread: Components related to Thread/OpenThread (openthread, thread_broadcast, etc.)

These components are only available in firmware versions prior to 1.1.13.0.